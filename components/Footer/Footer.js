import React from 'react';
import { Link } from '../../routes';
import { Container, Typography } from '@material-ui/core';

const Footer = () => (
  <footer>
    <Container maxWidth="lg">
      <Typography variant="h5">Asylum Connect</Typography>
    </Container>
  </footer>
);

export default Footer;
